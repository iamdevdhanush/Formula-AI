/* ============================================
   FORMULAAI — RACE ENGINEER PAGE (AI Chat)
   Wrapper that uses the AI chat component
   ============================================ */
import { createAIChatPage } from '../components/ai-chat.js';

export function createRaceEngineerPage() {
  return createAIChatPage();
}
